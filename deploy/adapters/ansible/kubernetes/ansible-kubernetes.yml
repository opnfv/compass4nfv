---

- hosts: all
  remote_user: root
  max_fail_percentage: 0
  roles:
    - pre-k8s

- hosts: all
  remote_user: root
  max_fail_percentage: 0
  roles:
    - setup-k8s-network

- hosts: all
  remote_user: root
  max_fail_percentage: 0
  roles:
    - install-k8s-dependence

- hosts: localhost
  remote_user: root
  max_fail_percentage: 0
  roles:
    - kargo
